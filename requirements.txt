# Core agent
dspy-ai>=2.5.0
litellm>=1.50.0

# Service + UI
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
streamlit>=1.36.0
requests>=2.31.0

# Data / analysis
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.20.0
scikit-learn>=1.4.0
joblib>=1.3.0

# Explainability (EBM glassbox ARS model + blackbox explainers)
interpret>=0.7.0

# RAG
pypdf>=4.0.0
databricks-vectorsearch>=0.40      # production vector store
# qdrant-client>=1.9.0             # optional: local dev store, as in AML-MultiAgent-RAG

# Databricks / MLOps (pyspark comes from the cluster runtime — not pinned here)
databricks-sql-connector>=3.1.0
mlflow>=2.14.0

# Dev
pytest>=8.0.0
httpx>=0.27.0
